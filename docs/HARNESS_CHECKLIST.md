# Harness checklist

This file is generated from `repo_harness.toml` and the current repo surface.
이 파일은 `repo_harness.toml`과 현재 저장소 surface를 기준으로 생성됩니다.

Do not edit this file manually. Rerun the harness installer or updater when the adapter changes.
이 파일은 수동으로 수정하지 말고, 어댑터가 바뀌면 하니스 installer 또는 updater를 다시 실행합니다.

- generated at / 생성 시각: `2026-05-05T13:53:36Z`

## Repo profile / 저장소 프로필

- repo type / 저장소 유형: `fullstack-web`
- production sensitive / 운영 민감도: `true`
- has MCP / MCP 사용 여부: `false`
- has realtime / 실시간 기능 여부: `true`
- has deploy / 배포 여부: `true`
- install profile / 설치 프로필: `frontier`
- enabled features / 활성 기능: `core, consultation, browser-containerized, frontier, secondary-lanes, termination-supervisor, creative-workflow, diagram-design, engineering-discipline, ui-reference`
- lane mode / 레인 모드: `orchestrated`
- primary lane / 기본 레인: `codex`
- enabled lanes / 활성 레인: `codex`
- runtime backend / 런타임 백엔드: `native`
- parallelism strategy / 병렬 전략: `maximum-safe-parallel`
- browser automation / 브라우저 자동화: `containerized:docker:playwright`
- consultation policy / consultation 정책: `best-effort`
- creative workflow / creative workflow: `enabled:claude->gemini`
- developer progress board / developer progress board: `disabled`
- diagram design / diagram design: `enabled:claude->diagram-generation`
- engineering discipline / engineering discipline: `enabled:vertical:feedback-loop-first`
- UI reference / UI reference: `enabled:lazyweb:warning-only`
- capability control / 권한 제어: `enabled`
- proof orchestration / 증명 오케스트레이션: `disabled`
- TDD mode / TDD 모드: `contract-first`
- eval mode / 평가 모드: `false`
- rollout mode / rollout 모드: `maximum-safe-parallel`
- release rehearsal / 릴리스 리허설: `none`
- cleanup policy / 정리 정책: `best-effort`

## Lane routing / 레인 라우팅

- mode / 모드: `orchestrated`
- primary / 기본: `codex`
- enabled / 활성화: `codex`
- runtime backend / 런타임 백엔드: `native`
- no secondary role routing declared / 보조 레인 역할 라우팅 없음

## What applies to this repo / 이 저장소에 적용되는 사항

- fill the repo-local adapter with exact local truth, not placeholders / 플레이스홀더가 아니라 실제 로컬 truth로 어댑터를 채우기
- use Codex shared skills for repeatable methods / 반복 가능한 방법은 Codex 공용 스킬 사용
- use the repo-local TDD contract for enforceable behavior changes / 강제 가능한 동작 변경에는 저장소 로컬 TDD 계약 사용
- if the repo is actually changing a large handwritten source file, name it in the planned write set and add a structured `## File cohesion plan` instead of a free-form justification / 저장소에서 실제로 큰 hand-written source file을 변경한다면 planned write set에 명시하고 자유 서술형 변명 대신 구조화된 `## File cohesion plan` 추가
- disposable screenshots, Playwright captures, delegated run outputs, and repo-local Codex history should be pruned unless they are retained with `retain-artifact:` in the active execution plan / 활성 실행 계획의 `retain-artifact:`로 보관하지 않는 한 임시 스크린샷, Playwright 캡처, delegated run output, 저장소 로컬 Codex history는 정리되어야 함
- use `bash scripts/harness/doctor_primary_runtime.sh` to validate the selected primary runtime / 선택한 기본 런타임 검증에는 `bash scripts/harness/doctor_primary_runtime.sh` 사용
- enter the selected primary runtime through `bash scripts/harness/run_primary_runtime.sh` / 선택한 기본 런타임 진입은 `bash scripts/harness/run_primary_runtime.sh` 사용
- run harness-owned browser automation through `bash scripts/harness/run_browser_automation.sh` so downstream repos do not open the host browser by default / 하니스가 소유하는 브라우저 자동화는 기본적으로 호스트 브라우저를 열지 않도록 `bash scripts/harness/run_browser_automation.sh`를 통해 실행
- respect `repo_harness.toml [capability_control]` for browser-external access and repo-mutating orchestration / 브라우저 외부 접근과 저장소 변이 오케스트레이션에는 `repo_harness.toml [capability_control]`를 준수
- this repo is orchestrator-first; keep `codex` as the mutation owner and use secondary lanes only for roles declared in `repo_harness.toml [lane]` / 이 저장소는 orchestrator-first이며 `codex`를 mutation owner로 유지하고 보조 레인은 `repo_harness.toml [lane]`에 선언된 역할에만 사용
- every non-trivial wave must leave behind both Claude and Gemini consultation artifacts, or explicit blocker artifacts, before the wave can claim completion / 모든 비사소한 웨이브는 완료를 주장하기 전에 Claude와 Gemini consultation 산출물 또는 명시적 blocker 산출물을 남겨야 함
- visible English or Korean writing/localization requires Claude consultation, and image/presentation/generation work requires Gemini consultation / 보이는 영어·한국어 문구·현지화는 Claude consultation이 필요하고 이미지·프레젠테이션·생성 작업은 Gemini consultation이 필요함
- when the creative workflow preset is enabled, route all writing through Claude, route image/video generation through Gemini, and keep user-facing copy in English and Korean together / creative workflow preset이 활성화되면 모든 writing은 Claude로, image/video generation은 Gemini로 라우팅하고 사용자 노출 copy는 영어와 한국어를 함께 유지
- when the diagram-design preset is enabled, use the diagram-design style for internal explanatory diagrams and reserve Mermaid/D2/PlantUML/generated graphs for source-of-truth diagrams / diagram-design preset이 활성화되면 내부 설명용 다이어그램은 diagram-design 스타일을 사용하고 source-of-truth 다이어그램은 Mermaid/D2/PlantUML/generated graph로 유지
- when the engineering-discipline preset is enabled, clarify risky ambiguity in bounded passes, diagnose bugs with a feedback loop first, keep shared terms in `CONTEXT.md`, use ADRs only for durable decisions, and prove non-trivial behavior with vertical TDD slices / engineering-discipline preset이 활성화되면 위험한 모호성은 bounded pass로 확인하고, 버그는 먼저 feedback loop로 진단하며, 공유 용어는 `CONTEXT.md`에 두고, ADR은 durable decision에만 사용하며, 비사소한 동작은 vertical TDD slice로 증명
- before non-trivial review, generate an AgentOS review cycle so the reviewer has a regenerated review context, PR-body draft, comments, and verdict artifact / 비사소한 리뷰 전에는 AgentOS review cycle을 생성해 reviewer가 재생성된 review context, PR 본문 초안, 코멘트, verdict artifact를 사용하도록 함
- when the ui-reference preset is enabled, ground UI/UX creation, critique, redesign, and polish in Lazyweb references when configured; if Lazyweb is unavailable, continue and record the skipped reference grounding / ui-reference preset이 활성화되면 UI/UX 생성, critique, redesign, polish를 가능한 경우 Lazyweb reference로 근거화하고, 사용할 수 없으면 계속 진행하되 reference grounding 생략을 기록
- this repo must declare runtime proof before completion claims / 이 저장소는 완료 주장 전에 runtime proof를 선언해야 함
- validate convergence and second-client behavior through the containerized browser wrapper when relevant / 필요할 때 수렴성과 second-client 동작을 컨테이너형 브라우저 래퍼로 검증
- keep `docs/exec-plans/active/HARNESS_PROGRESS_LEDGER.html` current so users can see plan progress in one shared place / 사용자가 하나의 공용 화면에서 계획 진행 상황을 볼 수 있도록 `docs/exec-plans/active/HARNESS_PROGRESS_LEDGER.html`을 최신 상태로 유지
- keep `docs/exec-plans/active/state-ledger.json`, `docs/exec-plans/active/WORKING_CONTEXT.md`, and `docs/exec-plans/active/SESSION_START.md` current so lane launches start from the same machine-backed state / 레인 실행이 같은 machine-backed 상태에서 시작되도록 `docs/exec-plans/active/state-ledger.json`, `docs/exec-plans/active/WORKING_CONTEXT.md`, `docs/exec-plans/active/SESSION_START.md`을 최신 상태로 유지

## Immediate checklist / 즉시 확인할 체크리스트

- [x] Harness version and ref are pinned / 하니스 버전과 ref가 고정됨
- [x] Repo-local `AGENTS.md` exists / 저장소 로컬 `AGENTS.md` 존재 `AGENTS.md`
- [x] Repo-local harness guide exists / 저장소 로컬 하니스 가이드 존재 `docs/HARNESS_GUIDE.md`
- [x] Core progress ledger exists / 코어 progress ledger가 존재함 `docs/exec-plans/active/HARNESS_PROGRESS_LEDGER.html`
- [x] Machine-backed state ledger exists / machine-backed state ledger가 존재함 `docs/exec-plans/active/state-ledger.json`
- [x] Working context and session-start artifacts exist / working context와 session-start artifact가 존재함 `docs/exec-plans/active/WORKING_CONTEXT.md, docs/exec-plans/active/SESSION_START.md`
- [x] Lane selection is internally consistent / 레인 선택이 내부적으로 일관됨 `repo_harness.toml [lane]`
- [x] Runtime backend selection is internally consistent / 런타임 백엔드 선택이 내부적으로 일관됨 `repo_harness.toml [runtime]`
- [x] Maximum-safe parallel contract is internally consistent / 최대 안전 병렬 계약이 내부적으로 일관됨 `repo_harness.toml [parallelism]`
- [x] Docker-first browser automation contract is internally consistent / Docker-first 브라우저 자동화 계약이 내부적으로 일관됨 `repo_harness.toml [browser_automation]`
- [x] Consultation policy is internally consistent / consultation 정책이 내부적으로 일관됨 `repo_harness.toml [consultation]`
- [x] Proof orchestration is disabled for this install profile / 이 설치 프로필에서는 proof orchestration이 비활성화됨
- [x] Primary runtime launcher and doctor wrappers exist / 기본 런타임 실행 및 doctor 래퍼가 존재함 `scripts/harness/`
- [x] Mandatory AgentOS pre-action update gate exists / mandatory AgentOS pre-action update gate가 존재함 `scripts/_agent_os/ensure_harness_current.py`
- [x] Containerized browser automation wrapper exists / 컨테이너형 브라우저 자동화 래퍼가 존재함 `scripts/harness/run_browser_automation.sh`
- [x] UI reference Lazyweb setup wrapper exists / UI reference Lazyweb setup 래퍼가 존재함 `scripts/harness/setup_ui_reference_lazyweb.sh`
- [x] Release-certification law is internally consistent / 릴리스 인증 법칙이 내부적으로 일관됨 `docs/exec-plans/active/release-certification`
- [x] Repo-local Codex config carries the automatic request router / 저장소 로컬 Codex 설정에 자동 요청 라우터가 있음 `.codex/config.toml`
- [x] Repo-local Codex config bounds history and routes transient state into `.codex-home` / 저장소 로컬 Codex 설정이 history를 제한하고 임시 상태를 `.codex-home`으로 보냄 `.codex/config.toml`
- [x] Codex shared skill surface exists / Codex 공용 스킬 surface 존재 `.agents/skills`
- [x] Repo-local Codex launcher and cleanup wrappers exist / 저장소 로컬 Codex 실행 및 정리 래퍼가 존재함 `scripts/codex/`
- [x] Codex shared skill `harness-executor` is present and enabled / Codex 공용 스킬 `harness-executor` 이 존재하고 활성화됨
- [x] Codex shared skill `harness-planner` is present and enabled / Codex 공용 스킬 `harness-planner` 이 존재하고 활성화됨
- [x] Codex shared skill `harness-reviewer` is present and enabled / Codex 공용 스킬 `harness-reviewer` 이 존재하고 활성화됨
- [x] Codex shared skill `harness-runtime-verifier` is present and enabled / Codex 공용 스킬 `harness-runtime-verifier` 이 존재하고 활성화됨
- [x] Codex shared skill `harness-mcp-auditor` is present and enabled / Codex 공용 스킬 `harness-mcp-auditor` 이 존재하고 활성화됨
- [x] Codex shared skill `harness-handoff` is present and enabled / Codex 공용 스킬 `harness-handoff` 이 존재하고 활성화됨
- [x] Codex shared skill `harness-diagram-designer` is present and enabled / Codex 공용 스킬 `harness-diagram-designer` 이 존재하고 활성화됨
- [x] Engineering discipline skill `harness-clarifier` is present and enabled / Engineering discipline 스킬 `harness-clarifier` 이 존재하고 활성화됨
- [x] Engineering discipline skill `harness-diagnose` is present and enabled / Engineering discipline 스킬 `harness-diagnose` 이 존재하고 활성화됨
- [x] Engineering discipline skill `harness-domain-language` is present and enabled / Engineering discipline 스킬 `harness-domain-language` 이 존재하고 활성화됨
- [x] UI reference skill `harness-ui-researcher` is present and enabled / UI reference 스킬 `harness-ui-researcher` 이 존재하고 활성화됨
- [x] Engineering domain context exists / Engineering domain context가 존재함 `CONTEXT.md`
- [x] Engineering ADR directory exists / Engineering ADR 디렉터리가 존재함 `docs/adr`
- [ ] Latest review cycle context exists for grounded review when review evidence is present / review evidence가 있는 경우 grounded review를 위한 최신 review cycle context가 존재함 `docs/exec-plans/active/review-cycles/watch-global-runtime/cycle-001`
- [x] At least one real local validation proof is declared / 실제 로컬 validation proof가 최소 1개 선언됨
- [x] Repo-local TDD contract is declared / 저장소 로컬 TDD 계약이 선언됨 `repo_harness.toml [tdd]`
- [x] Runtime proof is declared when deploy is enabled / 배포가 활성화된 경우 runtime proof가 선언됨
- [x] MCP contract and guide are complete when MCP is enabled / MCP가 활성화된 경우 MCP 계약과 guide가 완성됨
- [x] A real active execution plan file exists / 실제 활성 실행 계획 파일이 존재함 `agentos-adoption-plan.md, agentos-convergence.md, apple-watch-support.md, real-watch-e2e.md, watch-global-runtime.md`
- [x] Harness validation stays local-only by default / 하니스 검증이 기본적으로 local-only로 유지됨

## Missing now / 지금 비어 있는 항목

- none / 없음

## Creative workflow recommendations / Creative workflow 권장사항

- creative workflow recommends enabling the Claude lane for writing ownership
- creative workflow recommends enabling the Gemini lane for image/video generation
- creative workflow cannot audit bilingual user-facing copy until user_facing_copy_roots is set

## Diagram design recommendations / Diagram design 권장사항

- diagram-design recommends enabling the Claude lane for internal explanatory diagrams

## Engineering discipline recommendations / Engineering discipline 권장사항

- engineering-discipline review evidence for agentos-convergence.md has no review-cycle context; run prepare_review_cycle.py before non-trivial review or record why review grounding was skipped
- engineering-discipline review evidence for apple-watch-support.md has no review-cycle context; run prepare_review_cycle.py before non-trivial review or record why review grounding was skipped
- engineering-discipline review evidence for real-watch-e2e.md has no review-cycle context; run prepare_review_cycle.py before non-trivial review or record why review grounding was skipped

## UI reference recommendations / UI reference 권장사항

- ui-reference Lazyweb token env `LAZYWEB_MCP_TOKEN` is not set; run `bash scripts/harness/setup_ui_reference_lazyweb.sh` or export it locally

## Commands to run / 실행할 명령

- `python3 ../agentOS/scripts/harness/verify_harness.py --repo "$(pwd)"`
- `python3 ../agentOS/scripts/harness/verify_protocol.py --repo "$(pwd)"`
- `python3 ../agentOS/scripts/harness/verify_exec_plan.py --repo "$(pwd)" --stage start`
- `bash scripts/harness/update_progress_ledger.sh`
- `bash scripts/harness/run_declared_tdd_check.sh --phase green`
- `pnpm test`
- `bash scripts/harness/doctor_primary_runtime.sh`
- `bash scripts/harness/run_primary_runtime.sh`
- `bash scripts/harness/run_browser_automation.sh python3 <browser-check>.py`
- `bash scripts/harness/setup_ui_reference_lazyweb.sh --dry-run`
- `bash scripts/codex/clean_repo_artifacts.sh`
- `bash scripts/codex/bootstrap_repo_codex_home.sh`
- `bash scripts/codex/doctor_repo_codex_home.sh`
- `bash scripts/codex/run_repo_codex.sh`

## Read first / 먼저 읽을 문서

1. `docs/HARNESS_CHECKLIST.md`
2. `AGENTS.md`
3. `repo_harness.toml`
4. `docs/HARNESS_GUIDE.md`
5. `docs/exec-plans/active/state-ledger.json`
6. `docs/exec-plans/active/SESSION_START.md`
7. `../agentOS/docs/FILE_COHESION_POLICY.md`
8. `docs/exec-plans/active/HARNESS_PROGRESS_LEDGER.html`
9. `.codex/config.toml`
10. `.agents/skills/`
11. `CONTEXT.md`
12. the active execution plan under `docs/exec-plans/active/`
